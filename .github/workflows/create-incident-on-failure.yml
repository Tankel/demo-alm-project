name: Create Incident on CI Failure

permissions:
  contents: read
  issues: write

on:
  workflow_run:
    workflows: ["CI - Tests"]
    types:
      - completed

jobs:
  create-issue-if-failed:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' && github.event.workflow_run.head_branch == 'main' }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Crear archivo dinámico
        run: |
          echo "# CI Failure Incident" > incident.md
          echo "Commit: ${{ github.event.workflow_run.head_sha }}" >> incident.md

      - name: Crear Issue
        uses: peter-evans/create-issue-from-file@v4
        with:
          title: "[INCIDENT][CI FAILURE] Tests fallaron en main - ${{ github.event.workflow_run.head_sha }}"
          content-filepath: incident.md
          labels: |
            incident
            ci-failure
            triage-needed
